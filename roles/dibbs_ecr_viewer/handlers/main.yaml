---
# Handlers for dibbs_ecr_viewer role

- name: Restart and enable fail2ban
  ansible.builtin.systemd:
    name: fail2ban
    state: restarted
    enabled: true

- name: Restart and enable docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    enabled: true
