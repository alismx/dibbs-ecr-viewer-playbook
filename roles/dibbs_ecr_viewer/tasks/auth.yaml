---
# Authentication Configuration Tasks

- name: Read existing AUTH_PROVIDER from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "AUTH_PROVIDER=${AUTH_PROVIDER:-}"
  register: dibbs_ecr_viewer_existing_auth_provider
  failed_when: false
  changed_when: false

- name: Prompt for AUTH_PROVIDER
  ansible.builtin.pause:
    prompt: |
      AUTH_PROVIDER (Current: {{ dibbs_ecr_viewer_existing_auth_provider.stdout | default('') }}) (Valid options are 'ad' or 'keycloak'):
  register: dibbs_ecr_viewer_auth_provider_input

- name: Set AUTH_PROVIDER fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_auth_provider: "{{ dibbs_ecr_viewer_auth_provider_input.user_input }}"

- name: Add AUTH_PROVIDER to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'AUTH_PROVIDER="{{ dibbs_ecr_viewer_auth_provider }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_auth_provider | length > 0

- name: Read existing AUTH_CLIENT_ID from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "AUTH_CLIENT_ID=${AUTH_CLIENT_ID:-}"
  register: dibbs_ecr_viewer_existing_auth_client_id
  failed_when: false
  changed_when: false

- name: Prompt for AUTH_CLIENT_ID
  ansible.builtin.pause:
    prompt: |
      AUTH_CLIENT_ID (Current: {{ dibbs_ecr_viewer_existing_auth_client_id.stdout | default('') }}):
  register: dibbs_ecr_viewer_auth_client_id_input

- name: Set AUTH_CLIENT_ID fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_auth_client_id: "{{ dibbs_ecr_viewer_auth_client_id_input.user_input }}"

- name: Add AUTH_CLIENT_ID to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'AUTH_CLIENT_ID="{{ dibbs_ecr_viewer_auth_client_id }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_auth_client_id | length > 0

- name: Read existing AUTH_CLIENT_SECRET from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "AUTH_CLIENT_SECRET=${AUTH_CLIENT_SECRET:-}"
  register: dibbs_ecr_viewer_existing_auth_client_secret
  failed_when: false
  changed_when: false

- name: Prompt for AUTH_CLIENT_SECRET
  ansible.builtin.pause:
    prompt: |
      AUTH_CLIENT_SECRET (Current: {{ dibbs_ecr_viewer_existing_auth_client_secret.stdout | default('') }}):
  register: dibbs_ecr_viewer_auth_client_secret_input

- name: Set AUTH_CLIENT_SECRET fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_auth_client_secret: "{{ dibbs_ecr_viewer_auth_client_secret_input.user_input }}"

- name: Add AUTH_CLIENT_SECRET to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'AUTH_CLIENT_SECRET="{{ dibbs_ecr_viewer_auth_client_secret }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_auth_client_secret | length > 0

- name: Read existing AUTH_ISSUER from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "AUTH_ISSUER=${AUTH_ISSUER:-}"
  register: dibbs_ecr_viewer_existing_auth_issuer
  failed_when: false
  changed_when: false

- name: Prompt for AUTH_ISSUER
  ansible.builtin.pause:
    prompt: |
      AUTH_ISSUER (Current: {{ dibbs_ecr_viewer_existing_auth_issuer.stdout | default('') }}):
  register: dibbs_ecr_viewer_auth_issuer_input

- name: Set AUTH_ISSUER fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_auth_issuer: "{{ dibbs_ecr_viewer_auth_issuer_input.user_input }}"

- name: Add AUTH_ISSUER to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'AUTH_ISSUER="{{ dibbs_ecr_viewer_auth_issuer }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_auth_issuer | length > 0

- name: Read existing AUTH_SESSION_DURATION_MIN from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "AUTH_SESSION_DURATION_MIN=${AUTH_SESSION_DURATION_MIN:-}"
  register: dibbs_ecr_viewer_existing_auth_session_duration_min
  failed_when: false
  changed_when: false

- name: Prompt for AUTH_SESSION_DURATION_MIN (optional)
  ansible.builtin.pause:
    prompt: |
      AUTH_SESSION_DURATION_MIN (Current: {{ dibbs_ecr_viewer_existing_auth_session_duration_min.stdout | default('') }})
      (Optional: leave blank for defaults):
  register: dibbs_ecr_viewer_auth_session_duration_min_input

- name: Set AUTH_SESSION_DURATION_MIN fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_auth_session_duration_min: "{{ dibbs_ecr_viewer_auth_session_duration_min_input.user_input }}"

- name: Add AUTH_SESSION_DURATION_MIN to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'AUTH_SESSION_DURATION_MIN="{{ dibbs_ecr_viewer_auth_session_duration_min }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_auth_session_duration_min | length > 0

- name: Read existing NEXTAUTH_URL from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "NEXTAUTH_URL=${NEXTAUTH_URL:-}"
  register: dibbs_ecr_viewer_existing_nextauth_url
  failed_when: false
  changed_when: false

- name: Prompt for NEXTAUTH_URL
  ansible.builtin.pause:
    prompt: |
      NEXTAUTH_URL (Current: {{ dibbs_ecr_viewer_existing_nextauth_url.stdout | default('') }})
      (http(s)://DOMAIN:PORT/ecr-viewer/api/auth/):
  register: dibbs_ecr_viewer_nextauth_url_input

- name: Set NEXTAUTH_URL fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_nextauth_url: "{{ dibbs_ecr_viewer_nextauth_url_input.user_input }}"

- name: Add NEXTAUTH_URL to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'NEXTAUTH_URL="{{ dibbs_ecr_viewer_nextauth_url }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_nextauth_url | length > 0
