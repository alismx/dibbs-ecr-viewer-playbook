---
# SQL Server Configuration Tasks

- name: Read existing SQL_SERVER_USER from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "SQL_SERVER_USER=${SQL_SERVER_USER:-}"
  register: dibbs_ecr_viewer_existing_sql_server_user
  failed_when: false
  changed_when: false

- name: Prompt for SQL_SERVER_USER
  ansible.builtin.pause:
    prompt: |
      SQL_SERVER_USER (Current: {{ dibbs_ecr_viewer_existing_sql_server_user.stdout | default('') }}):
  register: dibbs_ecr_viewer_sql_server_user_input

- name: Set SQL_SERVER_USER fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_sql_server_user: "{{ dibbs_ecr_viewer_sql_server_user_input.user_input }}"

- name: Add SQL_SERVER_USER to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'SQL_SERVER_USER="{{ dibbs_ecr_viewer_sql_server_user }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_sql_server_user | length > 0

- name: Read existing SQL_SERVER_PASSWORD from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "SQL_SERVER_PASSWORD=${SQL_SERVER_PASSWORD:-}"
  register: dibbs_ecr_viewer_existing_sql_server_password
  failed_when: false
  changed_when: false

- name: Prompt for SQL_SERVER_PASSWORD
  ansible.builtin.pause:
    prompt: |
      SQL_SERVER_PASSWORD (Current: {{ dibbs_ecr_viewer_existing_sql_server_password.stdout | default('') }}):
  register: dibbs_ecr_viewer_sql_server_password_input

- name: Set SQL_SERVER_PASSWORD fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_sql_server_password: "{{ dibbs_ecr_viewer_sql_server_password_input.user_input }}"

- name: Add SQL_SERVER_PASSWORD to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'SQL_SERVER_PASSWORD="{{ dibbs_ecr_viewer_sql_server_password }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_sql_server_password | length > 0

- name: Read existing SQL_SERVER_HOST from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "SQL_SERVER_HOST=${SQL_SERVER_HOST:-}"
  register: dibbs_ecr_viewer_existing_sql_server_host
  failed_when: false
  changed_when: false

- name: Prompt for SQL_SERVER_HOST
  ansible.builtin.pause:
    prompt: |
      SQL_SERVER_HOST (Current: {{ dibbs_ecr_viewer_existing_sql_server_host.stdout | default('') }}):
  register: dibbs_ecr_viewer_sql_server_host_input

- name: Set SQL_SERVER_HOST fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_sql_server_host: "{{ dibbs_ecr_viewer_sql_server_host_input.user_input }}"

- name: Add SQL_SERVER_HOST to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'SQL_SERVER_HOST="{{ dibbs_ecr_viewer_sql_server_host }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_sql_server_host | length > 0

- name: Read existing DB_CIPHER from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "DB_CIPHER=${DB_CIPHER:-}"
  register: dibbs_ecr_viewer_existing_db_cipher
  failed_when: false
  changed_when: false

- name: Prompt for DB_CIPHER
  ansible.builtin.pause:
    prompt: |
      DB_CIPHER (Current: {{ dibbs_ecr_viewer_existing_db_cipher.stdout | default('') }}):
  register: dibbs_ecr_viewer_db_cipher_input

- name: Set DB_CIPHER fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_db_cipher: "{{ dibbs_ecr_viewer_db_cipher_input.user_input }}"

- name: Add DB_CIPHER to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'DB_CIPHER="{{ dibbs_ecr_viewer_db_cipher }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_db_cipher | length > 0

- name: Read existing METADATA_DATABASE_SCHEMA from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "METADATA_DATABASE_SCHEMA=${METADATA_DATABASE_SCHEMA:-}"
  register: dibbs_ecr_viewer_existing_metadata_database_schema
  failed_when: false
  changed_when: false

- name: Prompt for METADATA_DATABASE_SCHEMA
  ansible.builtin.pause:
    prompt: |
      METADATA_DATABASE_SCHEMA (Current: {{ dibbs_ecr_viewer_existing_metadata_database_schema.stdout | default('') }}) ('core' or 'extended'):
  register: dibbs_ecr_viewer_metadata_database_schema_input

- name: Set METADATA_DATABASE_SCHEMA fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_metadata_database_schema: "{{ dibbs_ecr_viewer_metadata_database_schema_input.user_input }}"

- name: Add METADATA_DATABASE_SCHEMA to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'METADATA_DATABASE_SCHEMA="{{ dibbs_ecr_viewer_metadata_database_schema }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_metadata_database_schema | length > 0

- name: Read existing METADATA_DATABASE_MIGRATION_SECRET from .env file
  ansible.builtin.shell: |
    source "{{ dibbs_ecr_viewer_env_file }}" 2>/dev/null || true
    echo "METADATA_DATABASE_MIGRATION_SECRET=${METADATA_DATABASE_MIGRATION_SECRET:-}"
  register: dibbs_ecr_viewer_existing_metadata_database_migration_secret
  failed_when: false
  changed_when: false

- name: Prompt for METADATA_DATABASE_MIGRATION_SECRET
  ansible.builtin.pause:
    prompt: |
      METADATA_DATABASE_MIGRATION_SECRET (Current: {{ dibbs_ecr_viewer_existing_metadata_database_migration_secret.stdout | default('') }})
      (generated if not provided):
  register: dibbs_ecr_viewer_metadata_database_migration_secret_input

- name: Set METADATA_DATABASE_MIGRATION_SECRET fact
  ansible.builtin.set_fact:
    dibbs_ecr_viewer_metadata_database_migration_secret: "{{ dibbs_ecr_viewer_metadata_database_migration_secret_input.user_input }}"

- name: Add METADATA_DATABASE_MIGRATION_SECRET to wizard file
  ansible.builtin.lineinfile:
    path: "{{ dibbs_ecr_viewer_wizard_file }}"
    line: 'METADATA_DATABASE_MIGRATION_SECRET="{{ dibbs_ecr_viewer_metadata_database_migration_secret }}"'
    create: true
    mode: '0644'
  when: dibbs_ecr_viewer_metadata_database_migration_secret | length > 0
